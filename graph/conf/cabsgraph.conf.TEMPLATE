WWW=/opt/cabsinterface
# Don't put spaces in pool names.
POOLS=(pool1 pool2 pool3)
DB=db_name
declare -A AGGREGATE
# Add custom pools here.
AGGREGATE["All pools"]="${POOLS[*]}"
AGGREGATE["Another pool"]="pool1 pool2"
